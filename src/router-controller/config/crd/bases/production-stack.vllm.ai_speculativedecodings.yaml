---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: speculativedecodings.production-stack.vllm.ai
spec:
  group: production-stack.vllm.ai
  names:
    kind: SpeculativeDecoding
    listKind: SpeculativeDecodingList
    plural: speculativedecodings
    singular: speculativedecoding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.speculationType
      name: Type
      type: string
    - jsonPath: .spec.draftModel
      name: Draft Model
      type: string
    - jsonPath: .spec.numSpeculativeTokens
      name: Tokens
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SpeculativeDecoding is the Schema for the speculativedecodings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SpeculativeDecodingSpec defines the desired state of SpeculativeDecoding
            properties:
              draftModel:
                description: DraftModel is the name of the draft model to use (for
                  draft, mlp, and eagle types)
                type: string
              draftModelConfig:
                description: DraftModelConfig contains configuration for the draft
                  model
                properties:
                  max_tokens:
                    description: MaxTokens is the maximum number of tokens to generate
                    pattern: ^\d+$
                    type: string
                  temperature:
                    description: Temperature controls randomness in generation
                    pattern: ^(0|1|0\.[0-9]+)$
                    type: string
                required:
                - max_tokens
                - temperature
                type: object
              draftTensorParallelSize:
                description: DraftTensorParallelSize is the tensor parallel size for
                  the draft model
                minimum: 1
                type: integer
              numSpeculativeTokens:
                description: NumSpeculativeTokens is the number of tokens to speculate
                maximum: 32
                minimum: 1
                type: integer
              promptLookupMax:
                description: PromptLookupMax is the maximum number of tokens to look
                  up in the prompt (for ngram type)
                maximum: 8
                minimum: 1
                type: integer
              speculationType:
                description: SpeculationType defines the type of speculative decoding
                  to use
                enum:
                - draft
                - ngram
                - mlp
                - eagle
                type: string
            required:
            - numSpeculativeTokens
            - speculationType
            type: object
          status:
            description: SpeculativeDecodingStatus defines the observed state of SpeculativeDecoding
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              draftPodName:
                description: DraftPodName is the name of the draft model pod
                type: string
              lastUpdated:
                description: LastUpdated is the last time the status was updated
                format: date-time
                type: string
              stats:
                description: Stats contains statistics about speculative decoding
                  performance
                properties:
                  acceptanceRate:
                    description: AcceptanceRate is the rate at which draft tokens
                      are accepted
                    pattern: ^(0|1|0\.[0-9]+)$
                    type: string
                  averageSpeedup:
                    description: AverageSpeedup is the average speedup achieved
                    pattern: ^[0-9]+(\.[0-9]+)?$
                    type: string
                  totalTokensGenerated:
                    description: TotalTokensGenerated is the total number of tokens
                      generated
                    format: int64
                    type: integer
                required:
                - acceptanceRate
                - averageSpeedup
                - totalTokensGenerated
                type: object
              targetPodName:
                description: TargetPodName is the name of the target model pod
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
