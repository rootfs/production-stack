---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: inferencecaches.production-stack.vllm.ai
spec:
  group: production-stack.vllm.ai
  names:
    kind: InferenceCache
    listKind: InferenceCacheList
    plural: inferencecaches
    singular: inferencecache
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .spec.cacheType
      name: Type
      type: string
    - jsonPath: .spec.cacheSize
      name: Size
      type: integer
    - jsonPath: .status.cacheStats.hitRate
      name: Hit Rate
      type: number
    - jsonPath: .status.cacheStats.totalEntries
      name: Entries
      type: integer
    - jsonPath: .status.cacheStats.memoryUsage
      name: Memory
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceCache is the Schema for the inferencecaches API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceCacheSpec defines the desired state of InferenceCache
            properties:
              cacheSize:
                description: CacheSize is the size of the cache in tokens
                format: int64
                minimum: 1
                type: integer
              cacheTTL:
                description: CacheTTL is the time-to-live for cache entries
                pattern: ^[0-9]+[smhd]$
                type: string
              cacheType:
                description: CacheType is the type of cache to use
                enum:
                - kv
                - attention
                - hybrid
                type: string
              disaggregatedPrefillConfig:
                description: DisaggregatedPrefillConfig defines the configuration
                  for disaggregated prefill
                properties:
                  connectorConfig:
                    additionalProperties:
                      type: string
                    description: ConnectorConfig is the configuration for the connector
                    type: object
                  connectorType:
                    description: ConnectorType is the type of connector to use for
                      KV cache transfer
                    enum:
                    - grpc
                    - redis
                    - custom
                    type: string
                  enabled:
                    description: Enabled enables disaggregated prefill
                    type: boolean
                  lookupBufferConfig:
                    description: LookupBufferConfig is the configuration for the lookup
                      buffer
                    properties:
                      evictionPolicy:
                        description: EvictionPolicy is the policy for evicting entries
                          from the buffer
                        enum:
                        - lru
                        - fifo
                        type: string
                      maxSize:
                        description: MaxSize is the maximum size of the lookup buffer
                        format: int64
                        type: integer
                    required:
                    - evictionPolicy
                    - maxSize
                    type: object
                  prefillInstanceType:
                    description: PrefillInstanceType is the type of instance to use
                      for prefill
                    enum:
                    - prefill
                    - decode
                    type: string
                required:
                - connectorType
                - enabled
                - prefillInstanceType
                type: object
              image:
                description: Image is the container image to use
                type: string
              kvCacheTransferPolicy:
                description: KVCacheTransferPolicy defines the policy for KV cache
                  transfer
                properties:
                  evictionThreshold:
                    description: EvictionThreshold is the maximum memory usage before
                      eviction
                    pattern: ^(0|1|0\.[0-9]+)$
                    type: string
                  thresholdHitRate:
                    description: ThresholdHitRate is the minimum hit rate required
                      to transfer KV cache
                    pattern: ^(0|1|0\.[0-9]+)$
                    type: string
                required:
                - evictionThreshold
                - thresholdHitRate
                type: object
              modelName:
                description: ModelName is the name of the model to cache
                type: string
              pddRef:
                description: PDDRef is an optional reference to a PrefillDecodingDisaggregation
                properties:
                  apiGroup:
                    description: APIGroup is the group for the resource being referenced
                    enum:
                    - production-stack.vllm.ai
                    type: string
                  kind:
                    description: Kind is the kind of resource being referenced
                    enum:
                    - InferenceService
                    type: string
                  name:
                    description: Name is the name of the referenced object
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              resources:
                description: Resources defines the resource requirements for the container
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              sdRef:
                description: SDRef is an optional reference to a SpeculativeDecoding
                properties:
                  apiGroup:
                    description: APIGroup is the group for the resource being referenced
                    enum:
                    - production-stack.vllm.ai
                    type: string
                  kind:
                    description: Kind is the kind of resource being referenced
                    enum:
                    - InferenceService
                    type: string
                  name:
                    description: Name is the name of the referenced object
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              tensorParallelSize:
                description: TensorParallelSize is the number of GPUs to use for tensor
                  parallelism
                format: int32
                maximum: 8
                minimum: 1
                type: integer
            required:
            - cacheSize
            - cacheTTL
            - cacheType
            - image
            - kvCacheTransferPolicy
            - modelName
            - resources
            - tensorParallelSize
            type: object
          status:
            description: InferenceCacheStatus defines the observed state of InferenceCache
            properties:
              cacheStats:
                description: CacheStats contains statistics about the cache
                properties:
                  hitRate:
                    description: HitRate is the current hit rate of the cache
                    pattern: ^(0|1|0\.[0-9]+)$
                    type: string
                  memoryUsage:
                    description: MemoryUsage is the current memory usage of the cache
                    format: int64
                    type: integer
                  totalEntries:
                    description: TotalEntries is the total number of entries in the
                      cache
                    format: int64
                    type: integer
                required:
                - hitRate
                - memoryUsage
                - totalEntries
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the last time the status was updated
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
